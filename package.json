{
  "name": "firenode-http-proxy",
  "version": "0.0.0",
  "private": true,
  "pm": "npm",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --reporter list tests/*.js"
  },
  "dependencies": {
    "fs-extra": "^0.12.0",
    "http-proxy": "^1.6.1",
    "send": "^0.10.1",
    "smi.cache": "^0.3.0",
    "waitfor": "^0.1.3"
  },
  "devDependencies": {
    "mocha": "^2.0.1",
    "request": "^2.47.0"
  },
  "config": {
    "pio.deploy.converter": {
      "name": "nodejs-lib"
    }
  }
}